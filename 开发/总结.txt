一、
	1.stm32f103   串口1使用72MHz   其余都是36MHz
	2.步进电机的使用，脉冲。
	3.Modbus rtu采集风速。
二、
	1.功能上应该早点验证。
	2.读Lora模块的说明书时耗时较多 ，注意Lora模块工作和空闲之间却换是需要一段时间的
	3.ADC和DAC使用的时候注意基准电压。   要查看有效位是多少位   本次是0-4095		12位
	4.串口总是触发中断，需要将接收数据赋值即可，
三、
	1.应该早点学会CubeMX软件。
	2.软件SPI通信，注意延时
	3.Lora模式切换，注意延时
	4.Lora二发一收，  如果发送同时发，易造成接受堵塞。  所以采用点名的方式，接收模块点一次名，发送模块回应一次。
	5.波特率问题，接收模式的比特率和设置的不匹配，    初步怀疑是晶振的问题，暂时没有找到原因。
四、
	1.485串口初始化的时候，注意串口的默认值
	https://blog.csdn.net/BitFu/article/details/110920682
	2.初始化的时候，最好都注意下   引脚的默认值
	3.工程问题，工程的搭建出现问题，重新换个工程
五、
	1.收获下降沿信号，数值加1.
	2.通过网口发送。
	3.客户端和服务端的端口修改。回去确定一下

开发代码完善：
1.跨越多个源文件的全局变量必须带所在文件的名称，局部变量小写加下划线命名，全局变量命名中的不同单词第一个字母应大写
2.不同文件夹相对隔离，仅仅通过必要的接口完成任务
3.尽量让一个模块只负责少量且具有关联性的接口的实现，并且减少与其他模块或类的依赖关系
4.封装内部实现细节，只有公开必要的信息，减少依赖关系
5.不同模块之间尽量避免相互依赖，如果不能，尽量单一依赖。
